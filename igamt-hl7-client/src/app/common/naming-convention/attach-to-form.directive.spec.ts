import { AttachToFormDirective } from './attach-to-form.directive';

describe('AttachToFormDirective', () => {
  it('should create an instance', () => {
    const directive = new AttachToFormDirective();
    expect(directive).toBeTruthy();
  });
});
