import { StickyPopoverDirective } from './sticky-popover.directive';

describe('StickyPopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyPopoverDirective();
    expect(directive).toBeTruthy();
  });
});
