import { TooltipTextOverflowDirective } from './tooltip-text-overflow.directive';

describe('TooltipTextOverflowDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipTextOverflowDirective();
    expect(directive).toBeTruthy();
  });
});
