import { NamingConventionDirective } from './naming-convention.directive';

describe('NamingConventionDirective', () => {
  it('should create an instance', () => {
    const directive = new NamingConventionDirective();
    expect(directive).toBeTruthy();
  });
});
